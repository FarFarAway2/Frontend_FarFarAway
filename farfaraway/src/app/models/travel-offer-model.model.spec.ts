import { TravelOfferModel } from './travel-offer-model.model';

describe('TravelOfferModel', () => {
  it('should create an instance', () => {
    expect(new TravelOfferModel()).toBeTruthy();
  });
});
